[project]
name = "motyve-us"
version = "0.1.0"
description = "Website for motyveLLC"
requires-python = ">=3.11"
readme = "README.md"
dependencies = [
    "fastapi>=0.135.1",
    "python-dotenv>=1.2.2",
]
